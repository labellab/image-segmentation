export default function () {}

// Get more infomation at http://www.easyrgb.com/en/math.php
function rgb2xyz(rgba: number[][][][], w: number, h: number) {
  const xyz = new Float32Array(3 * w * h),
    gamma = 2.4;
  // for (let i = 0; i < w*h; i += 1) {

  // }
  return xyz;
}

// Get more infomation at http://www.easyrgb.com/en/math.php
function xyz2lab() {}
